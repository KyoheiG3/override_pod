# melos_managed_dependency_overrides: domain,gateway,override_pod_annotation
dependency_overrides:
  domain:
    path: ../domain
  gateway:
    path: ../gateway
  override_pod_annotation:
    path: ../../packages/override_pod_annotation
